name: promote images

on:
  push:
    paths:
      - images/config.yaml
      - .github/workflows/promote-images.yaml
  schedule:
    - cron: "0 0 * * MON"
  workflow_dispatch: {}

permissions:
  id-token: write
  packages: write

jobs:
  promote:
    uses: BobyMCbobs/sample-ko-monorepo/.github/workflows/reusable-build.yml@main
